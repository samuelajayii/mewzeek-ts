import { motion } from 'framer-motion'

const SignUp = () => {
    return(
        <motion.div
        // initial={{x: "100vw"}}
        // animate={{x: 0}}
        // transition={{type: "spring", delay: 0.5, stiffness: 40, duration: 5}}
        >
            <form>

            </form>
        </motion.div>
    )
}

export default SignUp